# Phase 2 source-to-target map

Reference rechecked on 2026-08-04: `Nagi-ovo/gemini-voyager` `main` at
`b5ea58b60bc9a4178e5844e8585eecc0485bbf37`.

| Requested item                                 | Reference evidence                                                                                                                             | Smallest GPT-Voyager target                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ChatGPT Deep Research export                   | `src/pages/content/deepResearch/*` separates final-report export from visible thinking/activity-panel Markdown download                        | Not ported. ChatGPT already downloads final reports as Markdown, Word, and PDF. Visible activity/source export remains a possible separate feature, but Phase 2 closes without it and it must never be described as hidden chain-of-thought.                                                                                                                                                                                                                               |
| Activity / attention view                      | Latest upstream has `src/pages/content/folder/activityView.ts`                                                                                 | Not ported. Phase 2 closes without adding another Folder view or touching Folder persistence.                                                                                                                                                                                                                                                                                                                                                                               |
| Reply-complete system notification             | Latest upstream has `src/pages/content/responseNotification/*` plus a background notification bridge                                           | Completed as default-on, with explicit stored `false` preserved: freeze the original route at request start, observe the current ChatGPT SSE endpoint, survive same-tab conversation switches and ordinary tab changes, and open the frozen route from the system notification. The 6,329-byte raw / 2,654-byte gzip content chunk loads by default; the 3,417-byte MAIN-world bridge drains and discards SSE chunks incrementally. DOM fallback exists only during submit/generation and stops after completion or an 8-second failed-start timeout. No polling, response-body retention, migration, or persistent target data. |
| Long code-block collapse                       | Latest upstream has `src/pages/content/codeBlockCollapse/index.ts`                                                                             | Completed as a default-off 3.98 KB lazy chunk using ChatGPT's current `#code-block-viewer`/sticky-toolbar DOM; long blocks stay expanded until the user clicks the injected toggle, and Mermaid is excluded.                                                                                                                                                                                                                                                              |
| Configurable line height and paragraph spacing | Latest upstream has separate `chatLineHeight` and `chatParagraphSpacing` modules                                                               | Implemented locally as one default-off lazy `chatSpacing` chunk with two independent controls.                                                                                                                                                                                                                                                                                                                                                                            |
| Modal scroll-position memory                   | No generic upstream module; current Prompt Manager already preserves list scroll across its own rerenders                                      | Not ported because no remaining modal path was reproduced losing its scroll position.                                                                                                                                                                                                                                                                                                                                                                                       |
| Storage-quota warning                          | Current upstream has a 2,034-line service/card/manager/background/toast stack; this repo also has an inactive 349-line `StorageMonitor`          | Not ported. Live usage is local 256.64 KiB / 10 MiB (2.51%) and sync 841 B / 100 KiB (0.82%). The upstream stack is disproportionate, while the old local monitor uses `navigator.storage.estimate()` and minute polling instead of exact `chrome.storage.local/sync` quota reads. Timeline already keeps only 80 conversation caches by LRU; the user chose no change.                                                                                                         |
| Export enhancements not already present        | Focused comparison found ordinary export features already present locally; local lazy JSZip/html-to-image and ChatGPT compatibility are better | Keep the local export stack. Treat Deep Research as its own feature; do not copy the upstream export directory.                                                                                                                                                                                                                                                                                                                                                           |
| Redacted diagnostic report                     | Latest upstream has `DiagnosticsExportService.ts` and `DiagnosticsExportCard.tsx` for plugin support reports                                   | Evaluated and not ported. The upstream schema is plugin-specific; no GPT-specific allowlist/report contract was approved.                                                                                                                                                                                                                                                                                                                                                  |
| Folder/sidebar visual refinement               | Latest upstream Folder CSS uses the 2026 native sidebar axis, title hierarchy, action sizing, and section density                              | Completed as CSS-only adaptation: copied those layout rules, retained ChatGPT font inheritance and existing purple/dark-gray color variables, and left DOM/storage untouched.                                                                                                                                                                                                                                                                                             |

Protected throughout: existing keys, origins, formats, fallback order, dirty files, and the
v1.7.5 default appearance. New Phase 2 modules remain disabled or interaction-loaded until
the user enables or invokes them, except the explicitly approved default-on reply notification.
